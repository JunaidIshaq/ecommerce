groups:
  - name: product_service_alerts
    rules:
      - alert: HighErrorRate
        expr: rate(http_server_requests_seconds_count{status!~"2.."}[1m]) > 5
        for: 1m
        labels:
          severity: warning
        annotations:
          description: "High error rate detected on Product Service"
