apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - quota.yaml
  - network-policies.yaml
  # ServiceMonitor requires the Prometheus Operator CRDs (kube-prometheus-stack).
  # Re-enable after installing observability. See k8s/README.md (plan section 8).
  # - servicemonitor.yaml
