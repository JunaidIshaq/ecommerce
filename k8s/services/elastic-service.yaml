# elastic-service — auto-generated by scripts/gen-k8s-services.py
apiVersion: apps/v1
kind: Deployment
metadata:
  name: elastic-service
  namespace: shopfast
  labels:
    app: elastic-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: elastic-service
  template:
    metadata:
      labels:
        app: elastic-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh", "-c", "true"]
      containers:
        - name: elastic-service
          image: ghcr.io/shopfast/elastic-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8092
          envFrom:
            - configMapRef:
                name: shopfast-config
            - secretRef:
                name: shopfast-secrets
          env:
            - name: ELASTIC_URI
              value: "http://elasticsearch:9200"
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: LOGSTASH_HOST
              value: "logstash"
            - name: LOGSTASH_PORT
              value: "5000"
            - name: MANAGEMENT_HEALTH_ELASTICSEARCH_ENABLED
              value: "false"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: JWT_SECRET
            - name: KEYCLOAK_ISSUER_URI
              value: "http://keycloak:8180/realms/shopfast"
            - name: KEYCLOAK_JWK_SET_URI
              value: "http://keycloak:8180/realms/shopfast/protocol/openid-connect/certs"
            - name: SERVER_PORT
              value: "8092"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8092
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8092
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8092
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: elastic-service
  namespace: shopfast
spec:
  selector:
    app: elastic-service
  ports:
    - port: 8092
      targetPort: 8092
  type: ClusterIP
