# auth-service — auto-generated by scripts/gen-k8s-services.py
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: shopfast
  labels:
    app: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh", "-c", "until wget -q -O /dev/null http://eureka-server:8761/eureka/actuator/health; do echo waiting for eureka-server; sleep 3; done; echo eureka-server ready"]
      containers:
        - name: auth-service
          image: ghcr.io/shopfast/auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: shopfast-config
            - secretRef:
                name: shopfast-secrets
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/auth_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: shopfast-config
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: DB_PASSWORD
            - name: APP_PASSWORD_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: APP_PASSWORD_ENCRYPTION_KEY
            - name: KEYCLOAK_BASE_URL
              value: "http://keycloak:8180"
            - name: KEYCLOAK_REALM
              value: "shopfast"
            - name: KEYCLOAK_ADMIN_CLIENT_ID
              value: "shopfast-services"
            - name: USER_SERVICE_URL
              value: "http://user-service:8086"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: "kafka:9092"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: JWT_SECRET
            - name: KEYCLOAK_ISSUER_URI
              value: "http://keycloak:8180/realms/shopfast"
            - name: KEYCLOAK_JWK_SET_URI
              value: "http://keycloak:8180/realms/shopfast/protocol/openid-connect/certs"
            - name: SERVER_PORT
              value: "8087"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: shopfast
spec:
  selector:
    app: auth-service
  ports:
    - port: 8087
      targetPort: 8087
  type: ClusterIP
