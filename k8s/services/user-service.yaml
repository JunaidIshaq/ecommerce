# user-service — auto-generated by scripts/gen-k8s-services.py
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: shopfast
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh", "-c", "true"]
      containers:
        - name: user-service
          image: ghcr.io/shopfast/user-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: shopfast-config
            - secretRef:
                name: shopfast-secrets
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/user_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: shopfast-config
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: DB_PASSWORD
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"
            - name: EUREKA_URL
              value: "http://eureka-server:8761/eureka/eureka/"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: JWT_SECRET
            - name: KEYCLOAK_ISSUER_URI
              value: "https://shopfast.live/realms/shopfast"
            - name: KEYCLOAK_JWK_SET_URI
              value: "https://shopfast.live/realms/shopfast/protocol/openid-connect/certs"
            - name: SERVER_PORT
              value: "8086"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: shopfast
spec:
  selector:
    app: user-service
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
