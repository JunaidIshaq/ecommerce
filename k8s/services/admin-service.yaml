# admin-service — auto-generated by scripts/gen-k8s-services.py
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: shopfast
  labels:
    app: admin-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh", "-c", "true"]
      containers:
        - name: admin-service
          image: ghcr.io/shopfast/admin-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8093
          envFrom:
            - configMapRef:
                name: shopfast-config
            - secretRef:
                name: shopfast-secrets
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/admin_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: shopfast-config
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopfast-secrets
                  key: DB_PASSWORD
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: LOGSTASH_HOST
              value: "logstash"
            - name: LOGSTASH_PORT
              value: "5000"
            - name: MANAGEMENT_HEALTH_ELASTICSEARCH_ENABLED
              value: "false"
            - name: MANAGEMENT_HEALTH_MAIL_ENABLED
              value: "false"
            - name: EUREKA_URL
              value: "http://eureka-server:8761/eureka/eureka/"
            - name: USER_SERVICE_URL
              value: "http://user-service:8086"
            - name: ORDER_SERVICE_URL
              value: "http://order-service:8084"
            - name: CART_SERVICE_URL
              value: "http://cart-service:8088"
            - name: CATEGORY_SERVICE_URL
              value: "http://category-service:8082"
            - name: PRODUCT_SERVICE_URL
              value: "http://product-service:8081"
            - name: INVENTORY_SERVICE_URL
              value: "http://inventory-service:8083"
            - name: COUPON_SERVICE_URL
              value: "http://coupon-service:8089"
            - name: KEYCLOAK_ISSUER_URI
              value: "http://keycloak:8180/realms/shopfast"
            - name: KEYCLOAK_JWK_SET_URI
              value: "http://keycloak:8180/realms/shopfast/protocol/openid-connect/certs"
            - name: SERVER_PORT
              value: "8093"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8093
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8093
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8093
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: shopfast
spec:
  selector:
    app: admin-service
  ports:
    - port: 8093
      targetPort: 8093
  type: ClusterIP
